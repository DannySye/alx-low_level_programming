#include "main.h"

/**
*_isdigit - This will check for a number or not
*
*@c: a character to be used
*Return: is 1 and 0
*/
int _isdigit(int c)
{
	if ((c >= 48) && (c <= 57))
	return (1);
return (0);
}
